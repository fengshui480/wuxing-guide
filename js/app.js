document.addEventListener('DOMContentLoaded', function () {
  // Placeholder: verify lunar-javascript is loaded
  if (typeof Solar === 'undefined') {
    console.error('lunar-javascript failed to load');
    return;
  }
  console.log('lunar-javascript loaded successfully');
  // App initialization will be wired up here in future tasks
});
